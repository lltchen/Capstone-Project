const initState = {
    cat:"hi"
}

export const reducer = (state = initState,action) => {
   switch (action.type) {
        case "GET_USERS":
            
            break;
    
        default:
            return state
    }






}

